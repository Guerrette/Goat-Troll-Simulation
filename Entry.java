/**
 * taken directly from the book
 * Example of what every entry should contain
 * Used by the nested Entry Class
 * @author Patrick
 *
 * @param <K>
 * @param <V>
 */
public interface Entry<K, V> 
{
	 K getKey();
	 V getValue();
}
